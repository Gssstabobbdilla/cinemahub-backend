import { Component } from '@angular/core';

// Placeholder: se reemplaza por el flujo real (elegir función -> elegir asientos ->
// crear reserva) cuando construyamos esta feature.
@Component({
  selector: 'app-reservas-page',
  standalone: true,
  template: `<h2>Reservas</h2><p>Próximamente.</p>`
})
export class ReservasPageComponent {}